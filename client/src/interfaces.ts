export default interface Client {
    firstName: string,
    lastName: string,
    email: string,
    phone: string
}

export default interface Reservation {
    date: string,
    time: string,
    guestsNumber: string
}

